# Tool — Communication Channel

## Status
Exists (Vapi voice AI receptionist, per `project_brightside_voice_ai`).

## What this agent uses it for
Inbound channel. Receives:
- Voice call transcripts (live or post-call)
- Contact form submissions
- Email-based inquiries (when configured per location)

## Configuration per location
(Placeholder — each location has its own Vapi assistant + webhook endpoint.)
